# DEPRECATED — diganti oleh openapi-buek-copilot-github.yaml
#
# File ini hanya pointer agar link lama tidak membingungkan.
# Gunakan: docs/mygpt/openapi-buek-copilot-github.yaml
#
# Lihat setup: docs/mygpt/GITHUB_ACTION_SETUP.md

openapi: 3.0.1
info:
  title: DEPRECATED — use openapi-buek-copilot-github.yaml
  version: 0.0.0
  description: >
    This minimal schema is obsolete. Import openapi-buek-copilot-github.yaml instead.
servers:
  - url: https://api.github.com
paths:
  /repos/{owner}/{repo}/contents/{path}:
    get:
      operationId: getRepoFile
      summary: DEPRECATED get file — use getContents in openapi-buek-copilot-github.yaml
      parameters:
        - name: owner
          in: path
          required: true
          schema:
            type: string
            default: abdularief23
        - name: repo
          in: path
          required: true
          schema:
            type: string
            default: buek-core
        - name: path
          in: path
          required: true
          schema:
            type: string
        - name: ref
          in: query
          schema:
            type: string
            default: main
      responses:
        "200":
          description: File metadata and base64 content
